import * as Parse from 'parse/node';
import ParsePointerEncoder from '../src/ParsePointerEncoder';

ParsePointerEncoder.setParse(Parse);

describe('encode()', () => {
  test('should encode an object correctly', () => {
    //
  });
});
